# CLAUDE.md

## 项目概述
这是一个面向展会与活动推广的官网与报名管理系统，集成静态页面展示、观众/展商/志愿者/嘉宾报名流程以及后台管理接口，便于快速发布活动信息并收集报名数据。

## 技术栈
- 前端：原生 HTML / CSS / JavaScript
- 后端：Node.js + Express
- 数据库：MySQL（通过 mysql2 连接）
- 身份认证：JWT
- 关键依赖：cors、body-parser、express、jsonwebtoken、mysql2
- 部署方式：PM2 + Shell 部署脚本

## 常用命令
- 安装后端依赖：
  - `cd backend && npm install`
- 启动后端服务：
  - `cd backend && npm start`
- 查看服务健康状态：
  - `http://localhost:3000/api/health`
- 部署更新：
  - `./update-site.sh`
- 初始化数据库：
  - 依次执行 [backend/sql/init.sql](backend/sql/init.sql) 中的表结构脚本

## 代码规范
### 命名约定
- 变量、函数、方法使用 `camelCase`
- 常量使用 `UPPER_SNAKE_CASE`
- 文件名尽量使用小写，若涉及模块可使用 `camelCase` 或 `kebab-case`
- 路由、控制器、数据库相关逻辑保持语义清晰，避免过短的命名

### 文件组织结构
- 根目录存放静态页面与前端资源：`index.html`、`style.css`、`main.js`、`*.html`
- 后端代码统一放在 `backend/` 目录下：
  - `routes/`：接口路由定义
  - `controllers/`：业务逻辑处理
  - `config/`：数据库与环境配置
  - `sql/`：数据库初始化脚本
- 新增功能时优先遵循“路由 -> 控制器 -> 数据库查询”的分层方式，避免把业务逻辑散落到页面脚本中

### 提交信息格式
建议使用以下格式：
- `feat: ` 新增功能
- `fix: ` 修复问题
- `refactor: ` 重构代码
- `chore: ` 构建/依赖/脚本调整
- `docs: ` 文档更新

## 架构约束
### 必须遵守的设计原则
- 保持前端页面与后端接口职责分离，静态页面负责展示，接口负责数据处理
- 优先复用现有的路由、控制器和数据库访问模式，而不是在页面中直接引入新的逻辑
- 新增接口时必须遵循统一的返回格式：`code`、`msg`、`data`
- 敏感信息如管理员密码、JWT 密钥、数据库连接信息必须通过环境变量或服务配置提供，严禁硬编码

### 禁止的做法
- 不要引入大型前端框架或复杂构建系统，除非项目明确升级
- 不要在前端直接暴露或处理敏感业务逻辑
- 不要把数据库连接凭据写入源码或提交到仓库
- 不要拼接原始 SQL 语句执行用户输入，必须使用参数化查询
- 不要在未确认需求时随意修改现有页面结构或接口字段

### 安全红线
- 任何涉及管理员权限的接口必须经过认证
- 处理用户提交数据时必须进行必要的校验与清洗
- 避免泄露用户隐私信息，尤其是电话、邮箱、身份证等字段
- 变更部署相关脚本或环境变量时必须谨慎，避免影响线上服务

## 测试要求
- 当前项目尚未建立完整的自动化测试体系，优先采用手工回归与接口 smoke test
- 变更后端逻辑时，至少确认以下路径可正常访问：
  - `/api/health`
  - 关键报名接口
  - 管理后台相关接口
- 新增后端功能时，建议补充基础测试；若引入测试框架，应优先选择轻量级 Node 测试方案
- 对关键模块建议保持较高的回归覆盖率，至少优先覆盖认证、报名提交、列表查询等核心流程

## 部署流程
1. 确保 MySQL 数据库已创建，并执行 [backend/sql/init.sql](backend/sql/init.sql) 初始化表结构
2. 在服务器或目标环境中安装后端依赖：`cd backend && npm install`
3. 配置必要环境变量，例如：`PORT`、`ADMIN_PASSWORD`、`JWT_SECRET`
4. 启动服务：`node app.js` 或通过 PM2 管理进程
5. 通过部署脚本 [update-site.sh](update-site.sh) 拉取代码、安装依赖并重启服务
6. 部署完成后验证健康检查接口与关键报名接口是否可用
